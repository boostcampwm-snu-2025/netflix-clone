
export function Init(indicatorID, leftButtonID, rightButtonID) {
    const indicator = document.querySelector('${indicatorID}');
    const leftButton = document.querySelector('${leftButtonID}');
    const rightButton = document.querySelector('${rightButtonID}');
}
